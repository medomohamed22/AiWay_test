import {allowMethods,json,requestLocale,localize,requireAdminToken,db,getAvailableModels,getToolModelSettings} from './_lib.js';
export default async function handler(req,res){if(!allowMethods(req,res,['GET','POST']))return;const locale=requestLocale(req);try{await requireAdminToken(req);if(req.method==='GET')return json(res,200,{settings:await getToolModelSettings(),models:await getAvailableModels(),imageModels:[{id:'gemini-3.1-flash-image-preview',name:'Nano Banana 2'},{id:'gemini-3-pro-image-preview',name:'Nano Banana Pro'}]});const b=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});const allowed=['coding','summary','ads','writing','translate','study','business','image'];const settings={};for(const k of allowed)if(typeof b.settings?.[k]==='string'&&b.settings[k].length<100)settings[k]=b.settings[k];const {error}=await db().from('ai_settings').upsert({key:'tool_models',value:settings,updated_at:new Date().toISOString()},{onConflict:'key'});if(error)throw error;return json(res,200,{ok:true,settings:{...(await getToolModelSettings()),...settings}});}catch(e){console.error(e);return json(res,500,{error:localize(locale,'تعذر حفظ إعدادات النماذج.','Could not save model settings.')});}}
